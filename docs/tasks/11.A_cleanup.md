# Task 11.A — Backlog / Technischer Abschluss

**Zweck:** Alle autonom-fähigen Resttasks vor inhaltlicher Review-Arbeit abräumen (sauberer technischer Projekt-Stand).
**Autonomie:** Maximal autonom innerhalb Security-Grenzen. CC iteriert pytest+ruff+mypy selbst bis grün. Reine Coding-/Cleanup-Phase, kein LLM.
**Branch:** `feature/cleanup`. Ein Branch, **atomare Commits pro Arbeitspaket** (AP1…AP5), ein Merge am Ende.
**Pfade:** `$HOME`/absolut. **archive-before-delete** für alles in `data/` und für entfernte Code-Teile (kein blindes Löschen).
**Prozess-Regel:** **Kein main-Merge ohne wörtlich getipptes `FREIGABE`.**

Reihenfolge bindend (Abhängigkeiten): AP1 → AP2 → AP3 → AP4 → AP5. Jedes AP: implementieren, Tests grün, Commit, dann nächstes.

---

## ℹ STATUS — Start (autonom, kein STOP)

```bash
git -C $HOME/projects/aktiv/PKM-rebuild status        # erwartet: main clean, 0/0
python -m pipeline status
```

Branch `feature/cleanup` anlegen.

---

## AP1 — `scripts/_pkm_common.py` extrahieren (Drift-Risiko beseitigen)

**Problem (Handover §4 Backlog):** 8 duplizierte Helfer + duplizierte Enums in `draft_inventory.py` ↔ `pkm_triage.py`. E1 musste 3× nachgezogen werden → Drift-Risiko.

**Soll:**
- Gemeinsames Modul `scripts/_pkm_common.py` mit: den 8 duplizierten Helfern + zentralen Enums (`ALLOWED_TYPE` inkl. `gedanke`, `ALLOWED_DOC_ROLE`, `ALLOWED_STATUS`, `ALLOWED_REVIEW`, `ALLOWED_CONFIDENCE`, `ALLOWED_CATEGORIES`) + `canonical_ck_slug`/NFC-Slug-Logik.
- `draft_inventory.py`, `pkm_triage.py`, `check_frontmatter.py`, `apply_category_mapping.py`, `phase8_runner.py` importieren aus `_pkm_common.py` statt eigene Kopien.
- **Single Source of Truth** — keine Enum-Kopie mehr außerhalb `_pkm_common.py` (und der Pipeline-Pydantic-Schemas; diese bleiben kanonisch für die Pipeline, `_pkm_common` spiegelt sie für Skripte — wenn machbar: `_pkm_common` importiert aus `pipeline.schemas`, sonst Kommentar-Verweis + ein Konsistenz-Test).
- Verhalten unverändert (reines Refactor).

**Verifikation:**
- `python3 scripts/pkm_triage.py | tail -15` → unverändert (180 READY · 19 FRESH_RUN · 0/0).
- `python3 scripts/check_frontmatter.py` → 180 Stems, 0 Issues.
- Neuer Test `tests/test_pkm_common.py`: Enum-Konsistenz `_pkm_common` ↔ `pipeline.schemas` (kein Drift möglich).

Commit `refactor(scripts): extract _pkm_common, dedupe helpers+enums`.

---

## AP2 — Config-Vollprune (`pipeline/pipeline.config.yaml`)

**Problem:** Stage-1/2 sind DEPRECATED (Option B), Embedding-/HDBSCAN-Clustering verworfen. Config trägt tote Settings.

**Soll:**
- Entfernen: `temperature_stage1`, `temperature_stage2`, alle Stage-1/2-spezifischen Keys; verworfene Cluster-Settings (`enable_umap_hdbscan`, `umap`, `hdbscan`) sofern nirgends mehr referenziert.
- **Vor Entfernen:** `rg` über `pipeline/` + `scripts/` ob Key noch gelesen wird. Referenziert → nicht entfernen, stattdessen ℹ im Abschlussreport.
- `redundancy.embeddings`-Block: behalten falls Phase 6 noch läuft/getestet; nur prunen wenn tot.
- Pydantic-Config-Modell (falls vorhanden) entsprechend anpassen.

**Verifikation:**
- `python -m pipeline status` + `python -m pipeline run --sample 10 --dry-run` rc=0.
- Tests grün (Config-Parsing-Test falls vorhanden).

Commit `chore(config): prune deprecated stage1/2 + verworfene cluster-settings`.

---

## AP3 — mypy-Altlast bereinigen

**Problem:** mypy nur auf neuen Modulen clean; Altlast in `phase_2`, `phase_3`, `phase_6`, `phase_8` + `schemas.py`.

**Soll:**
- mypy-Fehler in genannten Modulen beheben (Type-Hints, Optional-Handling, Return-Types). **Keine Verhaltensänderung** — nur Typannotationen/Guards.
- Ziel: `mypy pipeline/ scripts/` clean (oder dokumentierte, begründete `# type: ignore[code]` mit Inline-Kommentar wo externe Lib untypisiert).

**Verifikation:**
- `mypy pipeline/ scripts/` → 0 Errors (oder nur begründete ignores).
- pytest unverändert grün (Verhalten nicht verändert).

Commit `fix(types): mypy-clean phase_2/3/6/8 + schemas`.

---

## AP4 — `unsortiert/`-Diagnose + `_index`-Klärung

**Zwei Teilaufgaben, beide read-mostly:**

**4a — `unsortiert/_index.md`:** Phase-10-Report meldet `unsortiert/` mit 8 Artikeln belegt, aber „14/14 _index". Prüfen ob `unsortiert/` ein `_index.md` hat.
- Genutzter Cluster mit ≥1 Artikel → soll `_index.md` bekommen (konsistent zu allen anderen genutzten Ordnern).
- Falls fehlt: Phase-9-Builder so anpassen, dass `unsortiert/` wie ein genutzter Cluster behandelt wird (Index ja), `00_Meta` weiterhin ausgeschlossen. Vault neu bauen (`build-vault --force`), verifizieren 0 Fails/0 Dups.

**4b — `unsortiert/`-Inhalt diagnostizieren (nicht verschieben):**
- Pro Artikel: `category`-Wert + warum kein 16-Ordner-Mapping griff (Mapping-Lücke vs. echte Business-Domäne ohne Ordner).
- Output: `data/02_pipeline_output/unsortiert_diagnose.md` — Tabelle: slug · category · Grund · Empfehlung (Mapping-Eintrag ergänzen / belassen / manuell zuordnen).
- **Nichts autonom verschieben** — das ist menschliche Kuratierungs-Entscheidung (⏸ REVIEW im App-Chat).

Commit `feat(vault): unsortiert _index + diagnose-report`.

---

## AP5 — E3: Intermediates archivieren

**Problem (Handover E3):** `.body.md` (180) + `.frontmatter.json` (180) + hidden `.meta.json` (156) in `03_drafts/` sind nach Phase 9 nur noch Ballast. Archivieren, nicht löschen.

**Soll:**
- `data/03_drafts/_archive_intermediates_<YYYYMMDD>/` anlegen.
- `.body.md` + `.frontmatter.json` + hidden `.meta.json` dorthin **verschieben** (move, nicht copy+delete; bleibt im selben Volume wiederherstellbar).
- Die finalen `.md`-Drafts (180) **bleiben** in `03_drafts/` (Provenance + möglicher Re-Review).
- `_archive_intermediates_*/README.md`: was/warum/wann, Wiederherstellungs-Hinweis.

**Verifikation:**
- `03_drafts/*.md` (ohne `.body.md`) weiterhin 180.
- `03_drafts/` enthält keine `.body.md`/`.frontmatter.json`/`.meta.json` mehr (außer im Archiv).
- `python3 scripts/pkm_triage.py` → unverändert 180 READY (Triage darf nicht auf Intermediates angewiesen sein; falls doch → STOP, nicht archivieren).

Commit `chore(drafts): archive phase8 intermediates (E3)`.

---

## Abschluss

- `docs/learnings/PHASE_11_cleanup.md`: was bereinigt, welche Configs entfernt, Drift-Risiko beseitigt, E3 archiviert.
- `docs/PROJECT_STATUS.md` aktualisieren: technischer Abschluss erreicht; verbleibend = manuelle DoD-Reste (siehe unten).
- pytest + ruff check + ruff format + `mypy pipeline/ scripts/` alle grün.

**🛑 STOP → App: main-Merge braucht wörtlich getipptes `FREIGABE`.** Davor Kurzreport pro AP (erledigt/geskippt+Grund), Test-/ruff-/mypy-Status, `unsortiert_diagnose.md`-Kernbefund, Archiv-Pfad.

---

## NICHT in diesem Task (manuell, muente — nur zur Info, CC tut hier nichts)

| Offen | Warum manuell |
|---|---|
| Backup 2. Medium (ext. SSD/Cloud) + Recovery-Drill | Hardware/Account-Zugriff, `07_backup_strategy.md` §2.2/§4 |
| Qualitätsstufe-2-Review aller 180 Vault-Files | inhaltliche Bewertung, über mehrere Sessions |
| `unsortiert/`-Artikel final zuordnen | Kuratierungs-Entscheidung (Input: `unsortiert_diagnose.md` aus AP4) |
| Projekt-Retro (`01_strategy.md` §10) | wenn DoD vollständig |

---

## Checkpoints (minimal)

| Wann | Signal |
|---|---|
| Triage/Frontmatter-Verifikation weicht nach Refactor/Archiv ab | 🛑 STOP → App |
| Triage hängt von Intermediates ab (AP5 würde Daten brechen) | 🛑 STOP → App, nicht archivieren |
| main-Merge | 🛑 wörtlich `FREIGABE` |

Alles andere: autonom durchziehen. AP einzeln skippen erlaubt, wenn Referenz-Check (AP2) oder Diagnose (AP4) das nahelegt — mit Begründung im Abschlussreport, kein STOP.
