"""PKM-Hilfsskripte (CLI-Tools rund um Pipeline, Triage, Frontmatter-Checks).

Als Package markiert, damit `scripts._pkm_common` der eindeutige Modulname ist
(Single Source of Truth, keine doppelte mypy-Auflösung). Die einzelnen Skripte
bleiben als `python3 scripts/<name>.py` ausführbar.
"""
